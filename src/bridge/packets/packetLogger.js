// bridge/packets/packetLogger.js
const logs = [];

export function logPacket({ type, variant, nodeId, timestamp, source, payload }) {
//   logs.push({ type, variant, nodeId, timestamp, source });
//   console.log(`[${variant}] ${nodeId} via ${type}: ${timestamp}`);
//   if (variant === 'metadata') {
//     console.log(payload);
//   }
}
