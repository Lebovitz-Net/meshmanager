# === Frontend API access ===
VITE_API_BASE_URL=https://api.mesh.local

# === WebSocket connection to bridge ===
VITE_WS_PORT=8080
VITE_WS_HOST=localhost
VITE_API_BASE=`http://${VITE_WS_HOST}:${VITE_WS_PORT}`

# === Optional: Node device IP (for display/debug only) ===
VITE_NODE_IP_HOST=192.168.1.52
VITE_NODE_IP_PORT=4403
